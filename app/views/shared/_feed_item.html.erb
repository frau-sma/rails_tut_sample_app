<article>
<div><%= link_to gravatar_for(feed_item.user, :size => 30), feed_item.user %>
<%= link_to feed_item.user.name, feed_item.user %></div>
<p class="content"><%= wrap(feed_item.content) %></p>
<p class="timestamp">Posted <%= time_ago_in_words(feed_item.created_at) %> ago.
<% if current_user?(feed_item.user) %>
<span class="delete-link">(<%= link_to 'delete', feed_item, :method => :delete, :confirm => 'Are you sure you want to delete your post?' %>)</span>
<% end %></p>
</article>
